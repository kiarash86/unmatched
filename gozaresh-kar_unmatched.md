# گزارش کار

## پیاده‌سازی دیجیتال بازی رومیزی Unmatched
### یک موتور بازی کارتی داده‌محور با ++C17، به‌همراه لایه‌ی نمایش گرافیکی مبتنی بر raylib، حریف هوش مصنوعی و قابلیت ذخیره/بارگذاری/Undo

**کیارش حبیبی**

تابستان ۱۴۰۴

---

## فهرست مطالب

1. [مقدمه](#۱-مقدمه)
2. [اهداف پروژه](#۲-اهداف-پروژه)
3. [معماری کلی پروژه](#۳-معماری-کلی-پروژه)
4. [الگوهای طراحی استفاده‌شده](#۴-الگوهای-طراحی-استفاده‌شده)
5. [مدیریت استثناها (Exception Handling)](#۵-مدیریت-استثناها-exception-handling)
6. [حریف هوش مصنوعی](#۶-حریف-هوش-مصنوعی)
7. [ذخیره، بارگذاری و Undo](#۷-ذخیره-بارگذاری-و-undo)
8. [نحوه‌ی Build و اجرای پروژه](#۸-نحوه‌ی-build-و-اجرای-پروژه)
9. [ساختار پوشه‌ها و توضیح فایل‌به‌فایل](#۹-ساختار-پوشه‌ها-و-توضیح-فایل‌به‌فایل)
10. [جمع‌بندی و نتیجه‌گیری](#۱۰-جمع‌بندی-و-نتیجه‌گیری)
11. [محدودیت‌های شناخته‌شده](#محدودیت‌های-شناخته‌شده)
12. [منابع (Resources)](#۱۱-منابع-resources)

---

## ۱. مقدمه

این گزارش، مستندسازی کامل پروژه‌ی «Unmatched Card Game Engine» است؛ یک پیاده‌سازی نرم‌افزاری از بازی رومیزی محبوب Unmatched — با سه شخصیت **Dracula**، **Sherlock Holmes** و **Invisible Man** — که با زبان ++C17 و کتابخانه‌ی گرافیکی سبک raylib نوشته شده است. هدف اصلی پروژه، طراحی و پیاده‌سازی یک موتور بازی ماژولار، قابل‌توسعه و داده‌محور بوده است؛ به‌گونه‌ای که قوانین، شخصیت‌ها، کارت‌ها و نقشه‌های بازی مستقیمًا در کد ++C هاردکد نشوند، بلکه از فایل‌های JSON زیر پوشه‌ی `/data` خوانده شوند.

این رویکرد داده‌محور، مزیت مهمی دارد: افزودن یک قهرمان، کارت یا نقشه‌ی جدید به بازی، صرفًا به معنای افزودن یک یا چند فایل JSON است و نیازی به تغییر، کامپایل مجدد یا حتی درک عمیق از کد منطق بازی ندارد. این جداسازی محتوا از کد، یکی از تصمیم‌های معماری محوری این پروژه بوده و در بخش‌های بعدی به‌تفصیل بررسی می‌شود.

نسبت به نسخه‌های اولیه‌ی پروژه، این نسخه دو قابلیت مهم را اضافه کرده است: یک **حریف هوش مصنوعی** مبتنی بر قانون که بازیکن ۲ را بدون نیاز به بازیکن انسانی دوم اداره می‌کند، و مکانیزم **ذخیره/بارگذاری/Undo** که کل وضعیت بازی را به‌صورت JSON سریالایز می‌کند. همچنین شخصیت سوم، **Invisible Man**، با مکانیزم ویژه‌ی توکن‌های مه به مجموعه‌ی قهرمانان قابل‌بازی اضافه شده است.

در ادامه‌ی این گزارش، ابتدا اهداف پروژه را مرور می‌کنیم، سپس معماری کلی و لایه‌بندی آن، الگوهای طراحی به‌کاررفته، مکانیزم مدیریت خطا، طراحی حریف هوش مصنوعی، مکانیزم ذخیره/بارگذاری/Undo، نحوه‌ی build و اجرای پروژه، و در نهایت ساختار پوشه‌ها و توضیح فایل‌به‌فایل آن را ارائه می‌دهیم. گزارش با جمع‌بندی، محدودیت‌های شناخته‌شده و فهرست منابع مورد استفاده در پروژه به پایان می‌رسد.

## ۲. اهداف پروژه

- پیاده‌سازی کامل قوانین بازی Unmatched شامل حرکت جنگجویان، نبرد هم‌زمان دو کارته، اثرات کارت‌ها و تشخیص شرایط پیروزی.
- طراحی معماری‌ای که منطق بازی (لایه‌ی Model/Engine) به‌طور کامل از رابط‌کاربری (لایه‌ی View) جدا باشد، تا تغییر در نحوه‌ی رسم صفحه هیچ تأثیری بر قوانین بازی نداشته باشد.
- داده‌محور کردن کامل محتوای بازی؛ به‌گونه‌ای که اضافه‌کردن قهرمان یا کارت جدید فقط با افزودن فایل JSON امکان‌پذیر باشد، بدون هیچ تغییری در کد ++C.
- طراحی یک سلسله‌مراتب استثنا (Exception) اختصاصی برای مدیریت خطاهای فایل، JSON و داده به‌شکلی ایمن، قابل‌ردیابی و قابل‌بازیابی در زمان اجرا.
- استفاده از الگوهای طراحی استاندارد (Factory، Observer، Singleton، Strategy، Dependency Injection، Memento) برای افزایش توسعه‌پذیری، خوانایی و نگهداری‌پذیری کد.
- افزودن یک **حریف هوش مصنوعی** برای بازیکن ۲، تا بازی بدون نیاز به دو بازیکن انسانی هم قابل‌اجرا باشد.
- فراهم‌کردن مکانیزم **ذخیره/بارگذاری** (تا ۳ اسلات) و **Undo** برای بازی، مبتنی بر سریالایز کردن کل وضعیت بازی به JSON.
- فراهم‌کردن یک تجربه‌ی کاربری گرافیکی کامل با raylib، شامل منوی اصلی، صفحه‌ی انتخاب قهرمان، صفحه‌ی بارگذاری بازی، و صحنه‌ی اصلی بازی با پشتیبانی از صدا، فونت و بافت اختصاصی.

## ۳. معماری کلی پروژه

کد پروژه به پنج لایه‌ی مجزا تقسیم شده که هرکدام مسئولیت مشخصی دارند. این جداسازی باعث می‌شود تغییر در یک لایه (مثلًا نحوه‌ی رسم صفحه) هیچ تأثیری بر منطق قوانین بازی نداشته باشد و برعکس.

| لایه | پوشه | توضیحات |
|---|---|---|
| **Model** | `model/` | کلاس‌های داده‌ای ساده: `Card`، `Fighter`/`Hero`/`Sidekick`، `Deck`، `Map`، `Tile`. این لایه فقط وضعیت و دسترسی‌های ساده دارد و هیچ قانونی از بازی در آن پیاده نشده است. |
| **Engine** | `engine/` | قطعات واقعی قوانین بازی: `Effect` (کاری که یک کارت انجام می‌دهد)، `Condition` (شرط اعمال‌شدن یک اثر)، `Query` (مقداری محاسبه‌شده که یک اثر می‌خواند، مثلًا تعداد کمک‌کارها) و `gameData` (زمینه‌ی مشترک که بین این سه رد و بدل می‌شود). |
| **Factory** | `factory/` | تبدیل فایل‌های JSON زیر `/data` به اشیای واقعی .../Hero/Card/Map/Effect. تنها لایه‌ای که فرمت داده روی دیسک را می‌شناسد؛ هر خطای احتمالی را می‌گیرد و به `FactoryException` تبدیل می‌کند. |
| **Controller** | `controller/` | `GameManager` (موتور قوانین / ماشین‌حالت نوبت‌ها، نبردها، ذخیره/بارگذاری و Undo)، `SceneManager` (صحنه‌ی فعال)، `PlayerSelectionManager` (انتخاب قهرمان پیش از شروع بازی)، `AIController` (حریف هوش مصنوعی)، `AudioManager`/`FontManager`/`TextureManager` (کش منابع)، و `app` (حلقه‌ی اصلی raylib). |
| **View** | `view/` | صحنه‌های رسم‌شده با raylib (`MainScene`، `HeroSelectionScene`، `LoadGameScene`، `GameScene`) و اجزای رابط‌کاربری کوچک (`Button`، `Particle`). صحنه‌ها فقط وضعیت `GameManager` را می‌خوانند و رسم می‌کنند؛ هیچ قانونی در آن‌ها پیاده نشده است. |

جریان داده برای قوانین بازی همواره یک‌طرفه است: فایل JSON ← Factory ← شیء Model ← Engine (Effect/Condition/Query) ← اعمال نتیجه توسط GameManager ← خواندن وضعیت و رسم آن توسط View. این جهت‌دهی مشخص، ردیابی خطا و افزودن قابلیت جدید را ساده می‌کند، چون هر تغییر مسیر مشخصی برای عبور از لایه‌ها دارد.

## ۴. الگوهای طراحی استفاده‌شده

### ۴.۱ Factory

به ازای هر نوع داده، یک کلاس Factory مجزا وجود دارد: `HeroFactory`، `CardFactory`، `DeckFactory`، `MapFactory`، `TileFactory`، `SidekickFactory`، `EffectFactory`، `ConditionFactory`، `QueryFactory` و `HeroInfoFactory`. همه‌ی این Factoryها ساختار یکسانی دارند: ورودی خام (یک JSON یا مسیر فایل) را می‌گیرند، یک شیء Model/Engine می‌سازند و بدنه را در try/catch قرار می‌دهند تا در صورت خطا، یک `FactoryException` با اطلاعات کامل پرتاب کنند. با این طراحی، افزودن یک قهرمان یا کارت جدید تنها به افزودن یک فایل JSON زیر `/data` نیاز دارد، بدون هیچ تغییری در کد ++C.

### ۴.۲ Observer

رابط `IGameObserver` به `GameScene` اجازه می‌دهد به رویدادهایی مانند `onGameStarted`، `onCardPlayed`، `onCombatResolved` و `onFighterDamaged` که توسط `GameManager` فراخوانی می‌شوند واکنش نشان دهد؛ بدون این‌که `GameManager` هیچ اطلاعی از لایه‌ی رسم داشته باشد. این جداسازی دقیقًا همان مرز بین Model/Engine و View را که در بخش معماری توضیح داده شد، در عمل پیاده می‌کند.

### ۴.۳ چندریختی به سبک Strategy

کلاس‌های `Effect`، `Condition` و `Query` هرکدام یک کلاس پایه‌ی انتزاعی هستند که پیاده‌سازی‌های مشخص زیادی دارند (مثلًا `dmgEffect`، `moveEffect`، `drawEffect` برای `Effect`، یا `isNearEnemyCondition` و `wonTheWarCondition` برای `Condition`) و در زمان بارگذاری، بر اساس فیلد `type` یا `name` در JSON، توسط Factory مربوطه ساخته می‌شوند. این یعنی رفتار هر کارت، بدون نیاز به شاخه‌های شرطی گسترده در کد، صرفًا با ترکیب این اشیای چندریخت تعیین می‌شود.

### ۴.۴ Singleton (به سبک Meyer's)

`PlayerSelectionManager` و `LoadGameSelection` تنها Singletonهای عمدی پروژه‌اند. اولی برای انتقال اطلاعات «کدام بازیکن کدام قهرمان را انتخاب کرده» از صفحه‌ی انتخاب قهرمان به داخل بازی استفاده می‌شود، و دومی اسلاتِ ذخیره‌ی درخواست‌شده در `LoadGameScene` را تا وقتی `GameScene` واقعًا آن را بارگذاری کند نگه می‌دارد. هر دو به‌صورت یک static محلی داخل یک متد کلاس پیاده شده‌اند، نه یک متغیر سراسری (global) عمومی؛ این انتخاب از مشکلات رایج Singletonهای کلاسیک (ترتیب مقداردهی اولیه‌ی نامشخص) جلوگیری می‌کند.

### ۴.۵ تزریق وابستگی (Dependency Injection)

کلاس `app` مالک `AudioManager`، `FontManager`، `TextureManager` و `SceneManager` به‌صورت `unique_ptr` است و اشاره‌گر خام آن‌ها را به هرکس که نیاز داشته باشد پاس می‌دهد؛ بدون این‌که هیچ‌کدام از این‌ها متغیر سراسری باشند. این رویکرد، تست‌پذیری و کنترل چرخه‌ی عمر منابع (به‌ویژه بافت‌ها و صداهای raylib) را بهبود می‌دهد.

### ۴.۶ Memento (برای ذخیره/بارگذاری و Undo)

متدهای `GameManager::serializeState()` و `buildFromState()` کل وضعیت بازی را به/از JSON تبدیل می‌کنند. همین سریالایز کردن، هم پایه‌ی مکانیزم Undo (نگه‌داشتن عکس‌فوری‌های وضعیت در `undoStack`) است و هم پایه‌ی ذخیره/بارگذاری روی دیسک؛ جزئیات آن در بخش [۷](#۷-ذخیره-بارگذاری-و-undo) آمده است.

## ۵. مدیریت استثناها (Exception Handling)

به‌جای این‌که خطاها بی‌صدا نادیده گرفته شوند یا برنامه با `std::terminate()` و بدون پیام مفید متوقف شود، یک سلسله‌مراتب استثنای اختصاصی در `include/utility/exceptions.h` تعریف شده است:

| استثنا | توضیح |
|---|---|
| `AppException` (پایه) | ریشه‌ی سلسله‌مراتب؛ همه‌ی استثناهای پروژه از این کلاس مشتق می‌شوند. |
| `FileException` | امکان باز کردن یا لیست‌کردن فایل/پوشه وجود ندارد. |
| `JsonParseException` | فایل باز شد اما محتوای آن JSON معتبر نیست. |
| `DataFormatException` | JSON با موفقیت پارس شد اما فیلد لازم وجود ندارد یا نوع آن اشتباه است. |
| `FactoryException` | یک Factory در ساخت شیء ناموفق بود؛ یکی از سه استثنای بالا را با اطلاعات «چه‌چیزی در حال ساخته‌شدن بود» بسته‌بندی می‌کند. |

هر لایه هنگام عبور استثنا به لایه‌ی بالاتر، اطلاعات بیشتری به آن اضافه می‌کند و لایه‌ی صحنه (Scene) جایی است که برنامه واقعًا بازیابی می‌شود، نه فقط لاگ می‌کند:

1. `utility/file.cpp` (توابع `load()` و `listFiles()`) در پایین‌ترین سطح، استثنای `FileException` یا `JsonParseException` پرتاب می‌کند.
2. هر `Factory::create()` خطاهای داخلی خودش را می‌گیرد و یک `FactoryException` با نام‌گذاری دقیق (مثلًا «ساخت قهرمان Dracula ناموفق بود: فیلد name در stats.json وجود ندارد») دوباره پرتاب می‌کند.
3. `GameManager::createFromSelection()` اطلاعات «نقشه‌ی X در حال آماده‌سازی بود» را به هر خطای Factory اضافه می‌کند.
4. `SceneManager::changeScene()` هر `AppException` ناشی از ساخت یک صحنه را می‌گیرد، آن را لاگ می‌کند و به‌جای کرش کل برنامه، به منوی اصلی برمی‌گردد؛ اگر ساخت خود منوی اصلی هم شکست بخورد، استثنا مجددًا پرتاب می‌شود.
5. `main()` آخرین لایه‌ی محافظتی است که هر خطای رسیدگی‌نشده را می‌گیرد و با پیامی واضح خارج می‌شود، به‌جای یک crash بی‌نام‌ونشان.

## ۶. حریف هوش مصنوعی

بازیکن ۲ اکنون می‌تواند توسط یک هوش مصنوعی مبتنی بر قانون (`AIController`) اداره شود، بدون نیاز به بازیکن انسانی دوم. بازیکن ۱ (هرکس که اول در صفحه‌ی انتخاب قهرمان انتخاب کند) همیشه انسان است؛ قهرمانی که بازیکن ۲ در همان صفحه انتخاب می‌کند، قهرمان AI می‌شود.

`AIController` هیچ وابستگی‌ای به raylib یا جزئیات داخلی `GameScene` ندارد؛ فقط از متدهای عمومیِ همان `GameManager` استفاده می‌کند (`startCombat`، `playCard`، `moveFighter`، `performManeuver`، `resolveCombat`، `submitTile`/`submitFighter`/`submitCard`/`submitEffectChoice`، `endTurn`، ...) — دقیقًا همان‌طور که لایه‌ی view برای انسان انجام می‌دهد. `GameScene::updateAI()` هر فریم یک‌بار `AIController::update()` را صدا می‌زند؛ اگر کاری برای انجام نباشد بی‌اثر است و هرگز به چیزی که متعلق به نوبت انسان است دست نمی‌زند.

کاری که هر بار نوبتش می‌شود انجام می‌دهد:

1. **انتخاب‌های در انتظار** (کاشی/جنگجو/کارت/اثر، چه از کارت‌های خودش چه از توانایی‌هایش): با اکتشافی‌های حریصانه‌ی ساده حل می‌شود — اگر امکان تمام‌کردن ضعیف‌ترین جنگجوی دشمن باشد همان، وگرنه کمک به زخمی‌ترین متحد، وگرنه نگه‌داشتن بهترین کارت به‌نظر، وگرنه اولین گزینه.
2. **دفاع در نبرد**، وقتی انسان حمله می‌کند: بازی‌کردن کارت دفاعیِ قانونی با بالاترین مقدار دفاع، یا تحمل ضربه اگر کارتی نداشته باشد. اگر کارت انتخاب‌شده نیاز به پیش‌بینی مقدار حمله داشته باشد (کارت «Elementary» شرلوک هولمز)، یک عدد تصادفی یکنواخت بین ۱ و مقدار چاپ‌شده‌ی کارت حمله حدس می‌زند، نه این‌که مقدار واقعی را مستقیم بخواند.
3. **نوبت خودش**، یک اکشن در هر مرحله:
   - حمله به نزدیک‌ترین جنگجوی دشمن قابل‌دسترس که بیشترین آسیب را می‌زند یا آن را تمام می‌کند، اگر حمله از قهرمان یا یک همراه زنده قانونی باشد.
   - وگرنه بازی‌کردن یک کارت رویداد («Scheme») قانونی، با اولویت کمک به زخمی‌ترین متحد یا تضعیف ضعیف‌ترین جنگجوی دشمن.
   - وگرنه مانور و حرکت به‌سمت نزدیک‌ترین جنگجوی دشمن.
   - وگرنه فقط مانور برای کشیدن کارت، سپس پایان نوبت پس از اتمام اکشن‌ها (اول دورریختن تا ۷ کارت در صورت نیاز).

این هوش مصنوعی عمداً ساده نگه داشته شده است: بدون پیش‌بینی چند نوبت جلوتر، بدون برنامه‌ریزی کل صفحه و بدون رفتار اختصاصی برای هر توانایی قهرمان فراتر از آنچه به‌طور عمومی در دسترس است (مثلًا «Blood Harvest» دراکولا را با نام نمی‌شناسد، فقط هر پرسش انتخاب-جنگجویی که آن توانایی مطرح می‌کند را مثل هر پرسش مشابه دیگر جواب می‌دهد). این AI یک حریف قابل‌بازی و نقطه‌ی شروع است، نه قوی؛ متدهای `takeTurnStep`/`tryAttack`/`tryPlayUsefulCard`/`tryMoveTowardEnemy` در `src/controller/AIController.cpp` جاهایی هستند که می‌توان آن را باهوش‌تر کرد.

## ۷. ذخیره، بارگذاری و Undo

- **ذخیره/بارگذاری** — `GameManager` تا `kSaveSlotCount` (۳) اسلات ذخیره را روی دیسک پشتیبانی می‌کند (`saveGame(slot)`/`loadGame(slot)`/`hasSave(slot)`/`saveFilePath(slot)`). کل وضعیت بازی به JSON سریالایز می‌شود. صحنه‌ی جدید `LoadGameScene` (از منوی اصلی، دکمه‌ی «LOAD GAME») اسلات‌های موجود را نشان می‌دهد و بازیکن یکی را انتخاب می‌کند؛ `LoadGameSelection` (یک Singleton سبک) اسلاتِ درخواست‌شده را تا وقتی `GameScene` واقعًا آن را بارگذاری کند نگه می‌دارد.
- **Undo** — `GameManager` پیش از هر اکشن معنادار یک عکس‌فوری JSON از وضعیت را در `undoStack` نگه می‌دارد (`canUndo()`/`undo()`). Undo در زمان انتخاب در انتظار، نبرد فعال یا اثرهای آغاز نوبت مسدود می‌شود تا وضعیت میانی خراب نشود.

## ۸. نحوه‌ی Build و اجرای پروژه

پیش‌نیازها: CMake نسخه‌ی ۳.۱۶ به بالا، یک کامپایلر با پشتیبانی از ++C17 و (در لینوکس) هدرهای توسعه‌ی X11/OpenGL که raylib به آن‌ها نیاز دارد (`libx11-dev`، `libxrandr-dev`، `libxinerama-dev`، `libxcursor-dev`، `libxi-dev`، `libgl-dev`). کتابخانه‌های raylib و nlohmann/json به‌صورت خودکار توسط CMake از طریق `FetchContent` دریافت می‌شوند و نیازی به نصب دستی آن‌ها نیست.

```bash
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . -j$(nproc)
./CardGameEngine
```

در زمان build، پوشه‌های `/assets` و `/data` به‌طور خودکار کنار فایل اجرایی کپی می‌شوند (طبق تنظیمات `CMakeLists.txt`)، بنابراین برنامه بدون نیاز به تنظیم مسیر دستی، بلافاصله پس از build قابل‌اجراست.

## ۹. ساختار پوشه‌ها و توضیح فایل‌به‌فایل

```
.
├── CMakeLists.txt
├── main.cpp
├── data/                      # داده‌های JSON بازی (قهرمان‌ها، دسته‌کارت‌ها، نقشه‌ها)
├── include/                   # هدرها، مطابق چیدمان src/ زیر
└── src/
    ├── controller/            # GameManager، AIController (موتور قوانین + AI)
    ├── factory/                # سازنده‌های JSON -> آبجکت مدل
    ├── model/                  # کلاس‌های داده‌ی خالص
    ├── utility/                 # I/O فایل + کمک‌کننده‌های استثنا
    └── view/scenes/             # صفحه‌های رسم‌شده با raylib
```

(`controller/AudioManager`، `FontManager`، `TextureManager`، `app`، `SceneManager`، `PlayerSelectionManager`، `LoadGameSelection` و هر هدر زیر `engine/` فقط‌هدر هستند، یعنی فایل `.cpp` متناظر ندارند.)

### ۹.۱ `include/controller` و `src/controller`

| فایل | توضیح |
|---|---|
| `app.h` | کلاس اصلی برنامه؛ مالک `SceneManager` و مدیرهای منابع (`Audio`/`Font`/`Texture`)، راه‌انداز پنجره‌ی raylib و اجراکننده‌ی حلقه‌ی اصلی برنامه (`run()`). فقط‌هدر. |
| `SceneManager.h` | مدیریت صحنه‌ی فعال و جابه‌جایی امن بین `MainScene`/`HeroSelectionScene`/`LoadGameScene`/`GameScene` با پشتیبانی از بازیابی خطا (رجوع کنید به بخش مدیریت استثناها). فقط‌هدر. |
| `PlayerSelectionManager.h` | تنها Singleton عمدی این بخش از پروژه. نگه‌دارنده‌ی انتخاب قهرمان هر بازیکن پیش از شروع بازی، تا `GameManager::createFromSelection()` بازی واقعی را از رویش بسازد. فقط‌هدر. |
| `GameManager.h`/`.cpp` | موتور قوانین بازی؛ ماشین‌حالت نوبت‌ها، محدودیت دو اکشن در هر نوبت، حرکت (Maneuver)، نبرد هم‌زمان دو کارته (`startCombat`/`resolveCombat`؛ دومی مقدار پیش‌بینی‌شده‌ی مدافع برای کارت «Elementary» شرلوک را هم می‌گیرد)، کارت‌های Scheme (`playCard`)، مکانیزم انتخاب معلق برای درخواست از بازیکن (انتخاب خانه/جنگجو/کارت/اثر)، تشخیص برنده، توانایی‌های ویژه‌ی شخصیت‌ها (Blood Harvest دراکولا، هم‌افزایی شرلوک/واتسون، توکن‌های مه‌ی Invisible Man)، و همچنین **ذخیره/بارگذاری** (`saveGame`/`loadGame`/`hasSave`) و **Undo** (`canUndo`/`undo`). `createFromSelection()` نقطه‌ی ورودی ساخت یک مسابقه‌ی آماده از روی انتخاب `PlayerSelectionManager` است. |
| `AudioManager.h` | کش منابع صوتی از `assets/music`/`assets/sounds` بر اساس `MusicID`/`SoundID`؛ هرکدام فقط یک‌بار بارگذاری و در فراخوانی‌های بعدی از کش بازگردانده می‌شوند. فقط‌هدر. |
| `FontManager.h` | کش فونت‌ها از `assets/fonts` بر اساس `FontID`، با یک فونت جایگزین اگر فونت خاصی موجود نباشد. فقط‌هدر. |
| `TextureManager.h` | کش بافت‌ها از `assets/images` بر اساس `TextureID`. فقط‌هدر. |
| `AIController.h`/`.cpp` | حریف هوش مصنوعیِ ساده و مبتنی بر قانون که قهرمان بازیکن ۲ را بازی می‌کند. جزئیات در بخش [۶](#۶-حریف-هوش-مصنوعی). |

### ۹.۲ `include/model` و `src/model`

این پوشه کلاس‌های داده‌ای خالص بازی را در بر دارد: `Card`، `Fighter`/`Hero`/`Sidekick`، `Deck`، `Map` و `Tile`، به‌همراه enumهای کمکی و ساختار `Player` برای مرحله‌ی انتخاب قهرمان.

| فایل | توضیح |
|---|---|
| `fighter.h`/`.cpp` | کلاس پایه برای هر چیزی که می‌تواند روی نقشه بایستد و بجنگد: نام، تصویر، سلامتی/حداکثر سلامتی، حرکت، نوع حمله (نزدیک/دور) و خانه‌ی فعلی. `Hero` و `Sidekick` هر دو از آن مشتق می‌شوند. |
| `hero.h`/`.cpp` | قهرمان یک بازیکن: کمک‌کارهای اضافه و `Deck` اختصاصی او. |
| `sidekick.h`/`.cpp` | کمک‌کار یک قهرمان (مثلًا خواهر Dracula یا Watson). فقط `getFighterType()` را روی sidekick تنظیم می‌کند. |
| `card.h`/`.cpp` | یک کارت واحد: نام، تصویر، نوع (attack/def/multipurpose/event)، زمان‌بندی رویداد، محدودیت اجراکننده، مقادیر حمله/دفاع/Boost و فهرست Effectهای آن. همچنین یک مقدار پیش‌بینی‌شده (`predictedValue`، توسط `resolveCombat` تنظیم و توسط `predictEffect.h` خوانده می‌شود) و پرچم قفل مقدار (`lockValue`/`isValueLocked`) که `setValueEffect.h` برای غیرقابل‌تغییرکردن یک مقدار استفاده می‌کند. |
| `deck.h`/`.cpp` | دسته‌ی کارت‌های یک قهرمان (draw pile / discard pile / دست بازیکن) با متدهای `shuffle()`، `draw()` و `discard()`. |
| `map.h`/`.cpp` | صفحه‌ی بازی: مجموعه‌ای از Tileها با کلید id، به‌همراه پرس‌وجوهایی مانند «خانه‌های درون فاصله‌ی N از خانه‌ی X» و «چه کسی روی خانه‌ی Y ایستاده»، به‌علاوه‌ی مدیریت توکن‌های مه (`fogTokenCountAt`، `hasFogToken`) برای توانایی Invisible Man. |
| `tile.h`/`.cpp` | یک خانه‌ی صفحه: id، همسایه‌ها، تگ‌ها (`startPoint`، `secretPassage`)، ناحیه‌ها و موقعیت آن روی صفحه‌نمایش. |
| `InfoHero.h` | داده‌ی نمایشی صرف برای صفحه‌ی انتخاب قهرمان (نام، نقش، آمار، توضیحات، متن توانایی، رنگ تم)؛ مجزا از کلاس اصلی `Hero` تا این اطلاعات نمایشی مدل درون‌بازی را حجیم نکند. |
| `Player.h` | انتخاب قهرمان یک بازیکن در مرحله‌ی انتخاب: اندیس بازیکن، شناسه‌ی `HeroList` انتخاب‌شده، نام نمایشی. توسط `PlayerSelectionManager` استفاده می‌شود. |
| `LoadGameSelection.h` | Singleton سبک که اسلاتِ ذخیره‌ی درخواست‌شده در `LoadGameScene` را نگه می‌دارد تا `GameScene` آن را واقعًا بارگذاری کند. |
| `heroList.h` | `enum class HeroList { Dracula, SherlockHolms, InvisibleMan }` — مجموعه‌ی ثابت قهرمانان قابل‌بازی؛ نام‌های enum دقیقًا با نام پوشه‌های `/data` مطابقت دارند تا Factoryها بتوانند مستقیمًا از آن استفاده کنند. |
| `typeOfAttack.h` | `enum class TypeOfAttack { melee, ranged }`. |
| `typeOfCard.h` | `enum class TypeOfCard { attack, multipurpose, def, event }`. |
| `typeOfEvent.h` | `enum class TypeOfEvent { during_combat, after_combat, before_combat, start_turn_self, all_the_time, none }` — این‌که اثر یک کارت کِی باید فعال شود. |
| `typeOfFighter.h` / `typeOfPerformer.h` | enumهای کمکی برای فیلتر کردن جنگجویان (hero/sidekick/fighter) و مشخص‌کردن این‌که چه کسی مجاز به بازی‌کردن یک کارت است. |
| `typeOfTile.h` | `enum class TypeOfTile { none, startPoint, secretPassage }`. |

### ۹.۳ `include/engine`

قطعات سازنده‌ی قوانین بازی؛ همه سرآمد (header-only) هستند و کلاس‌های نهایی توسط `EffectFactory`، `ConditionFactory` و `QueryFactory` از روی JSON ساخته می‌شوند.

| فایل/پوشه | توضیح |
|---|---|
| `gameData.h` | ساختار زمینه‌ی مشترکی که به هر `Effect::execute()`/`Condition::check()`/`Query::evaluate()` پاس داده می‌شود: self، target، enemy، برنده/بازنده‌ی آخرین نبرد، وضعیت توکن مه در آغاز نوبت، نقشه و callbackهایی برای انتخاب یک کاشی/جنگجو/کارت/اثر — چه خطاب به صاحب کارت، چه (برای `opponentChoiceEffect.h`) خطاب به حریف. |
| `observer/observer.h` | رابط `IGameObserver`؛ قالب الگوی Observer که `GameManager` آن را فرامی‌خواند (`onGameStarted`، `onCardPlayed`، `onFighterDamaged`، `onCombatResolved`، ...) تا `GameScene` بدون وابستگی `GameManager` به لایه‌ی نمایش، از رویدادها مطلع شود. |
| `effects/*.h` | پیاده‌سازی اثرات کارت‌ها: آسیب (`dmgEffect`)، کشیدن کارت (`drawEffect`)، حرکت (`moveEffect`)، حذف کارت (`removeCardEffect`)، غیرفعال‌کردن اثر/توانایی (`removeEffectEffect`)، خارج‌کردن جنگجو از صفحه (`removeFromBoardEffect`)، دیدن دست حریف (`seeHandEffect`)، جابه‌جایی موقعیت (`positionExchangeEffect`)، افزودن به یک مقدار (`addEffect`)، تغییر مقدار کارت با عدد ثابت یا با Boost (`changeValueEffect`؛ همان اثر عمومی «+N» که جایگزین دو کلاس قدیمی دفاع/تغییردهنده شده)، ثابت‌کردن مقدار کارت روی یک عدد مشخص (`setValueEffect`)، انتخاب خانه توسط بازیکن (`choosePlaceEffect`)، گذاشتن کارت(های) انتخابی روی بالای دسته‌ی کارت (`placeOnDeckEffect`)، انتخاب بین چند زیر-اثر (`chooseEffectEffect`)، پرسیدن از حریف برای پذیرفتن/ردکردن یک اثر (`opponentChoiceEffect`)، پیش‌بینی مقدار حمله برای کارت «Elementary» شرلوک هولمز (`predictEffect`) و احیای کمک‌کار (`reviveEffect`). |
| `conditions/*.h` | شرط پایه (`condition.h`)، شرط نزدیکی پارامتری‌شده به دشمن/هدف/یک قهرمان مشخص (`proximityCondition`، جایگزین سه کلاس قدیمی نزدیکی)، مجاورت دو جنگجوی هم‌تیمی (`isTeamAdjacentCondition`)، باخت یا برد آخرین نبرد/کل بازی (`combatOutcomeCondition`، جایگزین سه کلاس قدیمی باخت/برد) و ایستادن روی/شروع نوبت روی توکن مه برای Invisible Man (`isOnFogTileCondition`). |
| `queries/*.h` | مقادیر محاسبه‌شده مانند شمارش جنگجویان از یک نوع خاص (`countfighter`) و خواندن مقدار Boost یک کارت (`cardBoost`). |

### ۹.۴ `include/factory` و `src/factory`

همه‌ی Factoryها ساختار یکسانی دارند: ورودی خام (یک JSON یا مسیر فایل) را می‌گیرند، یک شیء Model/Engine می‌سازند و بدنه را در try/catch قرار می‌دهند تا در صورت خطا، یک `FactoryException` با اطلاعات کامل پرتاب کنند.

| فایل | توضیح |
|---|---|
| `heroFactory.h`/`.cpp` | ساخت `Hero` از `/data/<نام>/stats.json` به‌همراه کمک‌کارها (از طریق `SidekickFactory`) و دسته‌ی کارت‌ها (از طریق `DeckFactory`). |
| `sidekickFactory.h`/`.cpp` | ساخت یک `Sidekick` از فایل `/data/<نام>/sidekicks/<sidekick>.json`. |
| `deckFactory.h`/`.cpp` | ساخت `Deck` با بارگذاری هر فایل JSON کارت در پوشه‌ی `/deck` یک قهرمان (از طریق `CardFactory`)، گسترش هر کارت به تعداد `quantity` و بُرزدن (shuffle) نتیجه. |
| `cardFactory.h`/`.cpp` | ساخت یک `Card` از JSON آن: مقادیر عددی، enumهای اجراکننده/نوع/رویداد (با کمک `magic_enum`)، و فهرست اثرات آن (از طریق `EffectFactory`). |
| `effectFactory.h`/`.cpp` | بزرگ‌ترین Factory پروژه: نگاشت رشته‌ی `effect.type` در JSON (مانند `dmg`، `move`، `draw_card`، `revive` و ...) به زیرکلاس `Effect` متناظر، سپس اتصال Conditionها/Queryهای آن. |
| `conditionFactory.h`/`.cpp` | نگاشت نام یک شرط به زیرکلاس `Condition` متناظر؛ برای نام ناشناخته `nullptr` برمی‌گرداند (به معنای «بدون شرط»). |
| `queryFactory.h`/`.cpp` | نگاشت نام یک Query به زیرکلاس متناظر و اتصال شرط‌های آن. |
| `mapFactory.h`/`.cpp` | ساخت `Map` از `/data/maps/<نام>.json`: اعتبارسنجی فیلدهای لازم (`name`، `tiles`) و ساخت هر `Tile` از طریق `TileFactory`. |
| `tileFactory.h`/`.cpp` | ساخت یک `Tile` از JSON آن: id (اجباری)، همسایه‌ها، تگ‌ها، ناحیه‌ها و موقعیت روی صفحه‌نمایش. |
| `heroInfoFactory.h`/`.cpp` | ساخت `InfoHero` برای صفحه‌ی انتخاب قهرمان از فایل `view.json`، با اعتبارسنجی کامل فیلدهای لازم. |

### ۹.۵ `include/view` و `src/view`

| فایل | توضیح |
|---|---|
| `scenes/Scene.h` | کلاس پایه‌ی انتزاعی هر صحنه: `onEnter()`/`onExit()`/`Update()`/`Draw()`، به‌همراه دسترسی مشترک به مدیرهای صوت/فونت/بافت و `SceneManager`. |
| `scenes/MainScene.h`/`.cpp` | منوی اصلی: عنوان، نقل‌قول‌ها و دکمه‌های START GAME / LOAD GAME / COLLECTION / SETTINGS / EXIT GAME؛ همچنین محرک افکت ذرات پس‌زمینه. |
| `scenes/HeroSelectionScene.h`/`.cpp` | انتخاب قهرمان برای هر بازیکن (از طریق `HeroInfoFactory`/`InfoHero`) و پخش صداهای مرتبط به‌ترتیب، سپس واگذاری کنترل به `GameScene` پس از آماده‌شدن هر دو بازیکن. |
| `scenes/LoadGameScene.h`/`.cpp` | صفحه‌ی انتخاب اسلاتِ ذخیره؛ سه اسلات (`kSaveSlotCount`) را نشان می‌دهد، این‌که کدام‌یک ذخیره دارد را با `GameManager::hasSave()` می‌خواند، و اسلاتِ انتخاب‌شده را در `LoadGameSelection` ثبت می‌کند تا `GameScene` بارگذاری کند. |
| `scenes/GameScene.h`/`.cpp` | صفحه‌ی اصلی بازی؛ ساخت یک `GameManager` واقعی (چه با `createFromSelection()` برای بازی جدید، چه با `GameManager::loadGame()` برای ادامه‌ی بازی ذخیره‌شده)، رسم نقشه/دست کارت‌ها/دکمه‌های اکشن، و گوش‌دادن به رویدادها به‌عنوان `IGameObserver` برای همگام نگه‌داشتن نمایش (لاگ رویداد، نوار سلامتی و ...) با وضعیت بازی. |
| `ui/Button.h`، `ui/Particle.h` | ساختارهای ساده‌ی رابط‌کاربری: دکمه‌ی قابل‌کلیک با مستطیل برخورد و متن، و ذره‌ی پس‌زمینه با موقعیت/سرعت/شعاع/آلفا. |
| `enums/ScenesType.h`، `TextureID.h`، `FontID.h`، `MusicID.h`، `SoundID.h` | enumهای کمکی نمایش: صحنه‌ی فعال (`ScenesType`، شامل سه صحنه‌ی هنوز پیاده‌نشده‌ی `HowToPlay`/`Settings`/`Collection`)، شناسه‌ی بافت‌ها، فونت‌ها، موسیقی و صداها. |

### ۹.۶ `include/utility` و `src/utility`

| فایل | توضیح |
|---|---|
| `exceptions.h` | سلسله‌مراتب استثنای اختصاصی پروژه (رجوع کنید به بخش [۵](#۵-مدیریت-استثناها-exception-handling)) به‌همراه توابع کمکی `json_util` برای اعتبارسنجی فیلدهای JSON. |
| `file.h`/`.cpp` | دو تابع پایه: `load(path)` برای باز و پارس‌کردن یک فایل JSON، و `listFiles(folder)` برای لیست‌کردن فایل‌های یک پوشه؛ پایین‌ترین سطحی که می‌تواند `FileException` یا `JsonParseException` پرتاب کند. |

### ۹.۷ `include/libraries`

کتابخانه‌های تک‌فایلی شخص ثالث که مستقیمًا در پروژه قرار داده شده‌اند (نه از طریق CMake دریافت می‌شوند): `magic_enum.hpp` برای تبدیل enum به رشته و برعکس در زمان کامپایل (بدون نیاز به نگاشت دستی)، و `IconsFontAwesome6.h` برای کدهای آیکون Font Awesome 6 در فونت رابط‌کاربری.

### ۹.۸ `data/`

داده‌های بازی به‌طور کامل JSON هستند و هیچ محتوایی در ++C هاردکد نشده است:

```
data/
├── Dracula/
│   ├── stats.json          # سلامتی، حرکت، نوع حمله، فهرست کمک‌کارها
│   ├── view.json            # اطلاعات نمایشی صفحه‌ی انتخاب قهرمان
│   ├── sidekicks/*.json     # آمار هر کمک‌کار
│   └── deck/*.json          # یک فایل به ازای هر کارت یکتا
├── SherlockHolms/            # همان چیدمان Dracula/
├── InvisibleMan/              # همان چیدمان، بدون کمک‌کار؛ به‌جایش fogTokenList
│   ├── stats.json            #   (توکن‌های مه برای مکانیزم پنهان‌شدن/جابه‌جایی ویژه‌اش)
│   ├── view.json
│   └── deck/*.json
└── maps/
    ├── baskervilleManor.json
    └── sarpedonMap.json     # هرکدام: نام + فهرست کامل خانه‌ها (id، همسایه‌ها، تگ‌ها، ناحیه‌ها، موقعیت)
```

توانایی ویژه‌ی Invisible Man: تا زمانی که روی خانه‌ی دارای توکن مه بایستد، کارت‌های دفاعی‌اش +۱ می‌گیرند و می‌تواند مستقیم بین هر دو خانه‌ی دارای توکن مه حرکت کند (پیاده‌سازی در `Map::hasFogToken`/`fogTokenCountAt` و منطق مربوطه در `moveEffect.h`/`dmgEffect.h`).

## ۱۰. جمع‌بندی و نتیجه‌گیری

در این پروژه، یک موتور بازی کارتی برای Unmatched با تمرکز بر جداسازی مسئولیت‌ها میان لایه‌های Model، Engine، Factory، Controller و View پیاده‌سازی شد. رویکرد داده‌محور بودن محتوای بازی (خواندن قهرمان‌ها، کارت‌ها و نقشه‌ها از JSON) باعث شد افزودن محتوای جدید — از جمله شخصیت سوم، Invisible Man — بدون تغییر در کد ++C ممکن باشد. همچنین سلسله‌مراتب استثنای اختصاصی، مدیریت خطا را در تمام لایه‌ها به‌شکلی یکپارچه و قابل‌ردیابی درآورد و لایه‌ی صحنه امکان بازیابی نرم از خطا را بدون کرش کل برنامه فراهم کرد.

نسبت به نسخه‌ی اولیه‌ی پروژه، افزودن حریف هوش مصنوعی (`AIController`) بازی را برای یک بازیکن تنها هم قابل‌اجرا کرد، و مکانیزم ذخیره/بارگذاری/Undo (مبتنی بر الگوی Memento و سریالایز کردن کل وضعیت بازی به JSON) تجربه‌ی کاربری را کامل‌تر کرد. استفاده از الگوهای طراحی استاندارد مانند Factory، Observer، Strategy، Singleton، Dependency Injection و Memento، پروژه را برای توسعه‌های آینده (مانند افزودن قهرمان‌های جدید، نقشه‌های بیشتر، هوش مصنوعی قوی‌تر یا اثرات کارتی پیچیده‌تر) آماده نگه داشته است؛ چراکه هر یک از این الگوها مرز مشخصی میان مسئولیت‌ها ایجاد کرده و افزودن قابلیت جدید را به افزودن یک کلاس یا فایل JSON، به‌جای تغییر کدهای موجود، تبدیل می‌کند.

### محدودیت‌های شناخته‌شده

- صحنه‌های **Settings** و **Collection** در `ScenesType` تعریف شده‌اند و دکمه‌شان در منوی اصلی وجود دارد، اما `SceneManager::changeScene()` برای آن‌ها هنوز موردی ندارد؛ کلیک روی آن‌ها فعلًا اثری ندارد.
- صحنه‌ی **How to Play** هم به همین شکل تعریف شده ولی پیاده‌سازی نشده است.
- تنها دو نقشه (`baskervilleManor`، `sarpedonMap`) و سه قهرمان (Dracula، Sherlock Holmes، Invisible Man) موجودند؛ سایر شخصیت‌های نقل‌شده در نقل‌قول‌های منوی اصلی صرفًا تزئینی‌اند و قهرمان قابل‌بازی نیستند.
- هوش مصنوعی فقط بازیکن ۲ را پوشش می‌دهد؛ بازی AI-در-برابر-AI یا بیش از دو بازیکن پشتیبانی نمی‌شود، و AI فاقد پیش‌بینی چند نوبت جلوتر یا برنامه‌ریزی کل صفحه است.
- تست‌های خودکار (unit test) در این export مشاهده نشد؛ صحت‌سنجی عمدتًا از طریق اعتبارسنجی JSON در زمان بارگذاری و سلسله‌مراتب استثنا انجام می‌شود.

## ۱۱. منابع (Resources)

### ۱۱.۱ کتابخانه‌ها و ابزارهای نرم‌افزاری

| منبع | توضیح / کاربرد در پروژه |
|---|---|
| raylib — https://www.raylib.com/ | کتابخانه‌ی سبک و ساده‌ی گرافیکی برای رندر ۲بعدی، مدیریت پنجره، ورودی و صدا؛ زیربنای کل لایه‌ی View و مدیرهای منابع پروژه. |
| nlohmann/json — https://github.com/nlohmann/json | کتابخانه‌ی سرآمد ++C برای خواندن/نوشتن JSON؛ مبنای تمام Factoryها برای تبدیل فایل‌های `/data` به اشیای ++C، و همچنین مبنای سریالایز کردن وضعیت بازی برای ذخیره/بارگذاری/Undo. |
| CMake — https://cmake.org/ | ابزار مدیریت build؛ پیکربندی `FetchContent` برای دریافت خودکار raylib و nlohmann/json، و کپی خودکار پوشه‌های `/assets` و `/data`. |
| magic_enum — https://github.com/Neargye/magic_enum | کتابخانه‌ی تک‌فایلی برای تبدیل enum به رشته و برعکس در زمان کامپایل، بدون نیاز به نگاشت دستی؛ در سراسر Factoryها برای خواندن مقادیر enum از JSON استفاده شده است. |
| IconsFontAwesome6.h — https://github.com/juliettef/IconFontCppHeaders | مجموعه‌ی آیکون‌های Font Awesome 6 برای استفاده در فونت رابط‌کاربری بازی. |

### ۱۱.۲ مرجع بازی و مفاهیم طراحی

| منبع | توضیح |
|---|---|
| Unmatched Board Game — Restoration Games | بازی رومیزی اصلی که این پروژه پیاده‌سازی دیجیتال آن است؛ مرجع قوانین حرکت، نبرد، دسته‌ی کارت و شخصیت‌های Dracula، Sherlock Holmes و Invisible Man. |
| Design Patterns: Elements of Reusable Object-Oriented Software — Gamma, Helm, Johnson, Vlissides (Gang of Four) | مرجع کلاسیک الگوهای طراحی شیءگرا (Factory Method، Observer، Singleton، Strategy، Memento) که مبنای معماری فصل ۴ این گزارش قرار گرفته‌اند. |
| cppreference.com — https://en.cppreference.com/w/cpp/17 | مرجع استاندارد زبان و ویژگی‌های ++C17 (مانند `std::unique_ptr`، `std::optional` و structured bindings) که در سراسر کد پروژه استفاده شده‌اند. |

---

*پایان گزارش.*
